const
MECHA_X = 15@
MECHA_Y = 16@
MECHA_Z = 17@
MECHA_A = 18@
MECHA_MARKER = 19@
MECHA_ACTOR = $VERSUS_PEOPLE[2]
MECHA_ARMOR_PRICE = 25@
MECHA_WHEEL_PRICE = 26@
MECHA_ENGINE_PRICE = 27@
MECHA_REPLACE_WHEEL_PRICE = 28@
MECHA_REPAIR_PRICE = 24@
MECHA_HYDRA_PRICE = 10@
MECHA_PAINT_PRICE = 14@
MECHA_CAR_NUM = 21@
MECHA_CAR_SELL_PRICE = 20@
MECHA_PEDTYPE = $MECHA_PEDTYPE
MECHA_AGRED_FLAG = 18@
end

:MECHA
thread 'MECHA'

if or
$DEFENSE_CHALLENGE_FLAG == 1
$GAMEMODE_VERSUS == 1
then
end_thread
end

:MECHA_START

while true
wait 250

//////////////////////////////////////////////////////////////////////////////

if and
$ENTERED_BCAMP_NUMBER == 1
$SAVESTATUS == 0
UNITED_MAP == 0
$GAMEMODE_WAR == 0
    then
    MECHA_X = 745.19
    MECHA_Y = -1340.7515
    MECHA_Z = 14.5229    
    MECHA_A = 90.0 
    MECHA_STAND_X = 743.002
    MECHA_STAND_Y = -1339.2698
    MECHA_STAND_Z = 13.6114
    jump to @MECHA_CREATE
    end

if and
$ENTERED_BCAMP_NUMBER == 2
$SAVESTATUS == 0
UNITED_MAP == 0
$GAMEMODE_WAR == 0
    then
    MECHA_X = 1276.3778
    MECHA_Y = 156.4841
    MECHA_Z = 21.3077    
    MECHA_A = 90.0
    MECHA_STAND_X = 1270.8911
    MECHA_STAND_Y = 155.9712
    MECHA_STAND_Z = 20.2489
    jump to @MECHA_CREATE
    end

if and
$ENTERED_BCAMP_NUMBER == 3
$SAVESTATUS == 0
UNITED_MAP == 0
$GAMEMODE_WAR == 0
    then      
    MECHA_X = 1078.4781
    MECHA_Y = 1237.7504
    MECHA_Z = 10.8203    
    MECHA_A = 360.0      
    MECHA_STAND_X = 1083.6766
    MECHA_STAND_Y = 1242.5789
    MECHA_STAND_Z = 11.1989
    jump to @MECHA_CREATE
    end

if and
$ENTERED_BCAMP_NUMBER == 4
$SAVESTATUS == 0
UNITED_MAP == 0
$GAMEMODE_WAR == 0
    then
    MECHA_X = -2140.0186
    MECHA_Y = -144.3811
    MECHA_Z = 36.5156    
    MECHA_A = 90.0
    MECHA_STAND_X = -2142.1277
    MECHA_STAND_Y = -142.5801
    MECHA_STAND_Z = 36.6578
    jump to @MECHA_CREATE
    end
    
//////////////////////////////////////////////////////////////////////////////

if and
$ENTERED_BCAMP_NUMBER == 6
$SAVESTATUS == 0
UNITED_MAP == 1
$GAMEMODE_WAR == 0
    then      
    MECHA_X = -1391.4867
    MECHA_Y = 273.233
    MECHA_Z = 26.0997   
    MECHA_A = 360.0 
    MECHA_STAND_X = -1387.80803805
    MECHA_STAND_Y = 276.943459621
    MECHA_STAND_Z = 23.545
    jump to @MECHA_CREATE
    end

if and
$ENTERED_BCAMP_NUMBER == 7
$SAVESTATUS == 0
UNITED_MAP == 1
$GAMEMODE_WAR == 0
    then      
    MECHA_X = -2624.8145
    MECHA_Y = 1263.2137
    MECHA_Z = 11.0766    
    MECHA_A = 270.0        
    MECHA_STAND_X = -2629.46
    MECHA_STAND_Y = 1268.44
    MECHA_STAND_Z = 10.467
    jump to @MECHA_CREATE
    end

if and
$ENTERED_BCAMP_NUMBER == 8
$SAVESTATUS == 0
UNITED_MAP == 1
$GAMEMODE_WAR == 0
    then           
    MECHA_X = 62.5431
    MECHA_Y = 1505.7568
    MECHA_Z = 49.245    
    MECHA_A = 360.0   
    MECHA_STAND_X = 70.0825
    MECHA_STAND_Y = 1510.65
    MECHA_STAND_Z = 46.6684 
    jump to @MECHA_CREATE
    end    
    
//////////////////////////////////////////////////////////////////////////////

if and
$ENTERED_BCAMP_NUMBER == 9
$SAVESTATUS == 0
UNITED_MAP == 1
$GAMEMODE_WAR == 0
    then            
    MECHA_X = 1587.1542
    MECHA_Y = -1464.0343
    MECHA_Z = 7.2798   
    MECHA_A = 180.0  
    MECHA_STAND_X = 1591.99
    MECHA_STAND_Y = -1467.43
    MECHA_STAND_Z = 4.5568
    jump to @MECHA_CREATE
    end
    
if and
$ENTERED_BCAMP_NUMBER == 10
$SAVESTATUS == 0
UNITED_MAP == 1
$GAMEMODE_WAR == 0
    then   
    
    if
    STORIES_MAP == 0
    then              
    MECHA_X = 891.0209
    MECHA_Y = -633.0258
    MECHA_Z = 5.7196    
    MECHA_A = 360.0   
    MECHA_STAND_X = 894.61
    MECHA_STAND_Y = -628.942
    MECHA_STAND_Z = 3.0571
    else      
    MECHA_X = 922.3968
    MECHA_Y = -669.856
    MECHA_Z = 5.7298    
    MECHA_A = 0.434   
    MECHA_STAND_X = 925.984
    MECHA_STAND_Y = -665.772
    MECHA_STAND_Z = 3.0571
    end
     
    jump to @MECHA_CREATE
    end      

if and
$ENTERED_BCAMP_NUMBER == 11
$SAVESTATUS == 0
UNITED_MAP == 1
$GAMEMODE_WAR == 0
    then    
    
    if
    STORIES_MAP == 0
    then             
    MECHA_X = 2049.4766
    MECHA_Y = -1875.6796
    MECHA_Z = 9.7197    
    MECHA_A = 270.2187      
    MECHA_STAND_X = 2047.92
    MECHA_STAND_Y = -1876.91
    MECHA_STAND_Z = 11.015
    else      
    MECHA_X = 1980.0714
    MECHA_Y = -1915.8434
    MECHA_Z = 5.729    
    MECHA_A = 91.2655      
    MECHA_STAND_X = 1976.38
    MECHA_STAND_Y = -1919.33
    MECHA_STAND_Z = 3.115
    end
    
    jump to @MECHA_CREATE
    end  
    
//////////////////////////////////////////////////////////////////////////////    

if and
$ENTERED_CAMP_NUMBER == 3
$SAVESTATUS == 0
UNITED_MAP == 0
$GAMEMODE_WAR == 0
    then
    MECHA_X = 1112.7607
    MECHA_Y = -334.6753
    MECHA_Z = 73.9851    
    MECHA_A = 180.0
    MECHA_STAND_X = 1114.0042
    MECHA_STAND_Y = -341.1332
    MECHA_STAND_Z = 74.7254
    jump to @MECHA_CREATE
    end
    
//////////////////////////////////////////////////////////////////////////////    
    
if and
$ENTERED_CAMP_NUMBER == 6
USHER_STORY_FLAG == 1
$SAVESTATUS == 0
UNITED_MAP == 0
$GAMEMODE_WAR == 0
    then      
    MECHA_X = 2151.4626
    MECHA_Y = -2284.2036
    MECHA_Z = 14.7734   
    MECHA_A = 226.1526      
    MECHA_STAND_X = 2154.4629
    MECHA_STAND_Y = -2287.1523
    MECHA_STAND_Z = 15.3219
    jump to @MECHA_CREATE
    end 
    
//////////////////////////////////////////////////////////////////////////////       
    
if and
$ENTERED_CAMP_NUMBER == 8
$SAVESTATUS == 0
UNITED_MAP == 0
$GAMEMODE_WAR == 0
USHER_STORY_FLAG == 0
    then
    MECHA_X = -2187.8574
    MECHA_Y = 1034.9384
    MECHA_Z = 80.0078    
    MECHA_A = 180.0
    MECHA_STAND_X = -2196.77
    MECHA_STAND_Y = 1031.83
    MECHA_STAND_Z = 77.4
    jump to @MECHA_CREATE
    end
    
if and
$ENTERED_CAMP_NUMBER == 13
$SAVESTATUS == 0
UNITED_MAP == 0
$GAMEMODE_WAR == 0
    then
    MECHA_X = -328.2075
    MECHA_Y = 2665.9124
    MECHA_Z = 63.2015    
    MECHA_A = 180.0
    MECHA_STAND_X = -327.535
    MECHA_STAND_Y = 2663.31
    MECHA_STAND_Z = 60.7
    jump to @MECHA_CREATE
    end
    
//////////////////////////////////////////////////////////////////////////////    
    
if and
$ENTERED_CAMP_NUMBER == 4
$SAVESTATUS == 0
UNITED_MAP == 1
$GAMEMODE_WAR == 0
    then
    
    if
    STORIES_MAP == 0
    then
    MECHA_X = -2435.0054
    MECHA_Y = 1914.256
    MECHA_Z = 58.7854    
    MECHA_A = 0.0
    MECHA_STAND_X = -2436.38
    MECHA_STAND_Y = 1918.02
    MECHA_STAND_Z = 56.2
    else
    MECHA_X = -2428.9272
    MECHA_Y = 1913.7675
    MECHA_Z = 58.7815    
    MECHA_A = 90.0
    MECHA_STAND_X = -2436.38
    MECHA_STAND_Y = 1918.02
    MECHA_STAND_Z = 56.2
    end
    
    jump to @MECHA_CREATE
    end
    
if and
$ENTERED_CAMP_NUMBER == 1
$SAVESTATUS == 0
UNITED_MAP == 1
$GAMEMODE_WAR == 0
    then
    
    if
    STORIES_MAP == 0
    then
    MECHA_X = -1310.9525
    MECHA_Y = 1424.7655
    MECHA_Z = 16.1315    
    MECHA_A = 360.0
    MECHA_STAND_X = -1303.51
    MECHA_STAND_Y = 1428.89
    MECHA_STAND_Z = 13.57
    else
    MECHA_X = -1299.8499
    MECHA_Y = 1414.8691
    MECHA_Z = 16.0562    
    MECHA_A = 90.0
    MECHA_STAND_X = -1303.11
    MECHA_STAND_Y = 1418.3
    MECHA_STAND_Z = 13.4562
    end
    
    jump to @MECHA_CREATE
    end
    
if and
$ENTERED_CAMP_NUMBER == 9
$SAVESTATUS == 0
UNITED_MAP == 1
$GAMEMODE_WAR == 0
    then
    
    if
    STORIES_MAP == 0
    then
    MECHA_X = -403.0574
    MECHA_Y = 714.7279
    MECHA_Z = 13.1322    
    MECHA_A = 270.0
    MECHA_STAND_X = -398.224
    MECHA_STAND_Y = 707.97
    MECHA_STAND_Z = 10.5
    else
    MECHA_X = -388.7741
    MECHA_Y = 657.9073
    MECHA_Z = 11.8232    
    MECHA_A = 270.0
    MECHA_STAND_X = -380.485
    MECHA_STAND_Y = 663.856
    MECHA_STAND_Z = 9.24
    end
    
    jump to @MECHA_CREATE
    end
    
//////////////////////////////////////////////////////////////////////////////    
    
if and
$ENTERED_CAMP_NUMBER == 2
$SAVESTATUS == 0
UNITED_MAP == 1
$GAMEMODE_WAR == 0
    then
    MECHA_X = 1284.6792
    MECHA_Y = 322.2613
    MECHA_Z = 6.2309    
    MECHA_A = 180.0
    MECHA_STAND_X = 1283.53
    MECHA_STAND_Y = 313.966
    MECHA_STAND_Z = 3.65
    jump to @MECHA_CREATE
    end
    
if and
$ENTERED_CAMP_NUMBER == 3
$SAVESTATUS == 0
UNITED_MAP == 1
$GAMEMODE_WAR == 0
    then
    MECHA_X = 1056.9481
    MECHA_Y = -1531.1653
    MECHA_Z = 4.4398    
    MECHA_A = 280.0
    MECHA_STAND_X = 1067.89
    MECHA_STAND_Y = -1538.1
    MECHA_STAND_Z = 2.05
    jump to @MECHA_CREATE
    end
    
if and
$ENTERED_CAMP_NUMBER == 5
$SAVESTATUS == 0
UNITED_MAP == 1
$GAMEMODE_WAR == 0
    then
    MECHA_X = 2350.9097
    MECHA_Y = -53.5541
    MECHA_Z = 19.9245    
    MECHA_A = 170.0
    MECHA_STAND_X = 2353.65
    MECHA_STAND_Y = -60.968
    MECHA_STAND_Z = 17.2995
    jump to @MECHA_CREATE
    end
    
if and
$ENTERED_CAMP_NUMBER == 6
$SAVESTATUS == 0
UNITED_MAP == 1
$GAMEMODE_WAR == 0
    then
    MECHA_X = 2140.7676
    MECHA_Y = -2182.7319
    MECHA_Z = 12.2411    
    MECHA_A = 0.0
    MECHA_STAND_X = 2144.2
    MECHA_STAND_Y = -2176.05
    MECHA_STAND_Z = 9.6677
    jump to @MECHA_CREATE
    end                                            

end

:MECHA_CREATE

/////////////////////////////////////////

MECHA_PEDTYPE = 26

if
UNITED_MAP == 0
then

if
$ENTERED_BCAMP_NUMBER > 0
    then
    
    if
    $ENTERED_BCAMP_NUMBER == 4
    then
    0@ = 202
    MECHA_PEDTYPE = 25
    else
    0@ = 17499
    MECHA_PEDTYPE = 25
    end
    
    else
    0@ = 12784
    end
    
if and
USHER_STORY_FLAG == 1
$ENTERED_CAMP_NUMBER == 6
    then
    0@ = 9204
    end    

else

if
$UNITED_III_SPAWN == 0
    then
    
    if
    STORIES_MAP == 0
    then
    0@ = 15632
    else
    0@ = 16850
    end
    
    else
    
    if
    STORIES_MAP == 0
    then
    0@ = 15954
    else
    0@ = 15013
    end
    
    end 
    
if
$ENTERED_BCAMP_NUMBER > 0
    then
    MECHA_PEDTYPE = 25
    end        

end

/////////////////////////////////////////////////////////////

if
MECHA_RESPAWN_FLAG == 2
then

if
UNITED_MAP == 0
then
0@ = 17499
end

:MECHA_CREATE2

MECHA_PEDTYPE = 25
MECHA_RESPAWN_FLAG = 0
//0AD1: show_formatted_text_highpriority "GAY SHIT" time 5000
end

/////////////////////////////////////////////////////////////

if
UNITED_MAP == 0
then

if and
$ENTERED_BCAMP_NUMBER == 1
$BANDITS_ZONE1_CAPTURED == 1
then
0@ = 12784
MECHA_PEDTYPE = 26
end

if and
$ENTERED_BCAMP_NUMBER == 2
$BANDITS_ZONE2_CAPTURED == 1
then
0@ = 12784
MECHA_PEDTYPE = 26
end

if and
$ENTERED_BCAMP_NUMBER == 3
$BANDITS_ZONE3_CAPTURED == 1
then
0@ = 12784
MECHA_PEDTYPE = 26
end

if and
$ENTERED_BCAMP_NUMBER == 4
$BANDITS_ZONE4_CAPTURED == 1
then
0@ = 12784
MECHA_PEDTYPE = 26
end

else

if and
$ENTERED_BCAMP_NUMBER == 6
$BANDITS_ZONE1_CAPTURED == 1
then
MECHA_PEDTYPE = 26
end

if and
$ENTERED_BCAMP_NUMBER == 9
$BANDITS_ZONE2_CAPTURED == 1
then
MECHA_PEDTYPE = 26
end

if and
$ENTERED_BCAMP_NUMBER == 7
$BANDITS_ZONE3_CAPTURED == 1
then
MECHA_PEDTYPE = 26
end

if and
$ENTERED_BCAMP_NUMBER == 8
$BANDITS_ZONE4_CAPTURED == 1
then
MECHA_PEDTYPE = 26
end

if and
$ENTERED_BCAMP_NUMBER == 10
$BANDITS_ZONE5_CAPTURED == 1
then
MECHA_PEDTYPE = 26
end

if and
$ENTERED_BCAMP_NUMBER == 11
$BANDITS_ZONE6_CAPTURED == 1
then
MECHA_PEDTYPE = 26
end

end

/////////////////////////////////////////////////////////////

while not model.Available(0@)
wait 0
model.Load(0@)
mload
end

MECHA_ACTOR = actor.Create(MECHA_PEDTYPE,0@,MECHA_X,MECHA_Y,MECHA_Z)  
0D10: set_actor MECHA_ACTOR alpha 255 // IF and SET
0E24: fix_char MECHA_ACTOR ground 1 brightness DO_ACTORS_BRIGHT_CORR and_fade_in 1
if
CAN_AIM_AT_SURVIVORS == 0
then
0568: set_actor MECHA_ACTOR untargetable 1
end

actor.PutAtNoOffset(MECHA_ACTOR,MECHA_X,MECHA_Y,MECHA_Z)
actor.Angle(MECHA_ACTOR) = MECHA_A
MECHA_AGRED_FLAG = 0 
Actor.SetWalkStyle(MECHA_ACTOR, "surv5")
Actor.Health(MECHA_ACTOR) = 1000     
Actor.WeaponAccuracy(MECHA_ACTOR) = 90

0816: set_actor MECHA_ACTOR dont_chase_victim 1
060B: set_actor MECHA_ACTOR decision_maker_to $DEC_ZOMBIE
07DD: set_actor MECHA_ACTOR attack_rate 80 // previously known as temper_to  
0946: set_actor MECHA_ACTOR actions_uninterupted_by_weapon_fire 1
081A: set_actor MECHA_ACTOR weapon_skill_to 2    
0446: set_actor MECHA_ACTOR dismemberment_possible 0 
04D8: set_actor MECHA_ACTOR drowns_in_water 0
0350: set_actor MECHA_ACTOR maintain_position_when_attacked 1
060F: set_actor MECHA_ACTOR melee_accuracy_to 200.0

MECHA_MARKER = Marker.CreateIconAndSphere2(63, MECHA_X, MECHA_Y, MECHA_Z)

while not model.Available(#MECHWRENCH)
wait 0
model.Load(#MECHWRENCH)
mload
end

actor.GiveWeaponAndAmmo(MECHA_ACTOR,106,1)

087E: set_actor MECHA_ACTOR weapon_droppable 0

while true
wait 0

if and
075C:   marker MECHA_MARKER enabled
0118:   actor MECHA_ACTOR dead
then
marker.Disable(MECHA_MARKER)

0A96: 0@ = actor MECHA_ACTOR struct
0A8E: 0@ = 0@ + 0x764
0A8D: 1@ = read_memory 0@ size 2 virtual_protect 1
0A8D: 2@ = read_memory 0xB6F5F0 size 2 virtual_protect 1

if
0800:   in_two_players_mode
then
0A8D: 0@ = read_memory 0xB7CF28 size 2 virtual_protect 1
else
0@ = -69
end

if or
003B:   1@ == 2@  // (int)
003B:   1@ == 0@  // (int)
then

if
KARMA_SYS_ON_FLAG == 1
then

if
CARMA_VERY_BAD_FLAG == 0
then

if
not $SOUND_EFFECT1_FLAG == 1
then                
CARMA_BAD_POINTS += 20
Audiostream.Loop($KARMA_BAD,0)
Audiostream.PerformAction($KARMA_BAD, PLAY)
Audiostream.Volume($KARMA_BAD) = $SOUNDS_VOLUME
CARMA_GOOD_POINTS -= 6
else
CARMA_BAD_POINTS -= 5
CARMA_GOOD_POINTS += 10
Audiostream.Loop($KARMA_GOOD,0)
Audiostream.PerformAction($KARMA_GOOD, PLAY)
Audiostream.Volume($KARMA_GOOD) = $SOUNDS_VOLUME
end

else
CARMA_BAD_POINTS += 10
CARMA_GOOD_POINTS -= 5
Audiostream.Loop($KARMA_THUG,0)
Audiostream.PerformAction($KARMA_THUG, PLAY)
Audiostream.Volume($KARMA_THUG) = $SOUNDS_VOLUME

if
not $ENTERED_CAMP_NUMBER == -1
then

if
$CAMP_DEAD_FLAG[$ENTERED_CAMP_NUMBER] == 1
    then
    BASE_TEAMMATE_AGRED = 1
    end

else
BCAMP_AGRED_FLAG = 1
end

end
                
end

end

end

if
MECHA_RESPAWN_FLAG == 1
then
//0AD1: show_formatted_text_highpriority "1" time 2000
MECHA_RESPAWN_FLAG = 0
marker.Disable(MECHA_MARKER)
actor.DestroyInstantly(MECHA_ACTOR)
jump to @MECHA_START
end

if
MECHA_RESPAWN_FLAG == 2
then
//0AD1: show_formatted_text_highpriority "2" time 2000
marker.Disable(MECHA_MARKER)
actor.DestroyInstantly(MECHA_ACTOR)
jump to @MECHA_START
end

if
MECHA_SHIT_FLAG == 2
then             
jump to @MECHA_BCAMP_CHECK
end

if or
MECHA_PEDTYPE == 26
MECHA_SHIT_FLAG == 1
then
if or
$SAVESTATUS == 1
$ENTERED_CAMP_NUMBER == -1
    then
//    0AD1: show_formatted_text_highpriority "3" time 2000
    MECHA_SHIT_FLAG = 0
    marker.Disable(MECHA_MARKER)
    actor.DestroyWithFade(MECHA_ACTOR)
    jump to @MECHA_START
    end
else
:MECHA_BCAMP_CHECK
if or
$SAVESTATUS == 1
$ENTERED_BCAMP_NUMBER == 0
    then
//    0AD1: show_formatted_text_highpriority "4" time 2000
    MECHA_SHIT_FLAG = 0
    marker.Disable(MECHA_MARKER)
    actor.DestroyWithFade(MECHA_ACTOR)
    jump to @MECHA_START
    end
end

if
MECHA_PEDTYPE == 26
then    
if and
$CAMP_AGRED_FLAG[$ENTERED_CAMP_NUMBER] == 0
$GAMEMODE_WAR == 0   
$PANEL_OPEN == 0
not actor.Driving(PLAYER_ACTOR)
8A32: not actor PLAYER_ACTOR on_turret_of_car
not actor.Dead(MECHA_ACTOR)
not USHER_STORY_FEATRUES_LOCKED == 1
    then
        
    if and
    0104:   actor PLAYER_ACTOR near_actor MECHA_ACTOR radius 3.0 3.0 1.0 sphere 0
    0364:   actor MECHA_ACTOR spotted_actor PLAYER_ACTOR
        then
        
        if and
        $KEY_USE == 69
        8A4B: not  controls_set_to_joystick
        then
        if
        8A2A: not  text_box 'GAME564' displayed
            then
            0512: show_permanent_text_box 'GAME564'
            end    
        else
            
        if
        8A4B: not  controls_set_to_joystick
        then
            
        if
        8A2A: not  text_box 'AME564B' displayed
            then
            0512: show_permanent_text_box 'AME564B'
            end
                
        else
            
        if
        8A2A: not  text_box 'AME564C' displayed
            then
            0512: show_permanent_text_box 'AME564C'
            end
            
        end   
                
        end
            
        else
            
        if or
        0A2A:   text_box 'GAME564' displayed
        0A2A:   text_box 'AME564B' displayed
        0A2A:   text_box 'AME564C' displayed
        then
        text.RemoveTextBox() 
        end
            
        end
    end
else
if and
BCAMP_AGRED_FLAG == 0
not ZONE_SIREN_FLAG == 1
$GAMEMODE_WAR == 0   
$PANEL_OPEN == 0
not actor.Driving(PLAYER_ACTOR)
8A32: not actor PLAYER_ACTOR on_turret_of_car
not actor.Dead(MECHA_ACTOR)
    then
        
    if and
    0104:   actor PLAYER_ACTOR near_actor MECHA_ACTOR radius 3.0 3.0 1.0 sphere 0
    0364:   actor MECHA_ACTOR spotted_actor PLAYER_ACTOR
        then
        
        if
        8A2A: not  text_box 'GAME564' displayed
        then
        0512: show_permanent_text_box 'GAME564' 
        end
            
        else
            
        if
        0A2A:   text_box 'GAME564' displayed
        then
        text.RemoveTextBox() 
        end
            
        end
    end
end

if
MECHA_PEDTYPE == 26
then    
if
056D:   actor MECHA_ACTOR defined
    then
    
    if and
    MECHA_AGRED_FLAG == 0
    $CAMP_AGRED_FLAG[$ENTERED_CAMP_NUMBER] == 1
    then                               
    077A: set_actor MECHA_ACTOR acquaintance 4 to_actors_pedtype 0 // see ped.dat
    077A: set_actor MECHA_ACTOR acquaintance 4 to_actors_pedtype 1 // see ped.dat
    0350: set_actor MECHA_ACTOR maintain_position_when_attacked 0
    05E2: AS_actor MECHA_ACTOR kill_actor PLAYER_ACTOR
    MECHA_AGRED_FLAG = 1
    end
    
    if and
    MECHA_AGRED_FLAG == 0
    BASE_TEAMMATE_AGRED == 1
    then                               
    077A: set_actor MECHA_ACTOR acquaintance 4 to_actors_pedtype 0 // see ped.dat
    077A: set_actor MECHA_ACTOR acquaintance 4 to_actors_pedtype 1 // see ped.dat
    0350: set_actor MECHA_ACTOR maintain_position_when_attacked 0
    05E2: AS_actor MECHA_ACTOR kill_actor PLAYER_ACTOR
    MECHA_AGRED_FLAG = 1
    end
                    
    if and
    $CAMP_AGRED_FLAG[$ENTERED_CAMP_NUMBER] == 0    
    $GAMEMODE_WAR == 0   
    $PANEL_OPEN == 0
    0AB0:   key_pressed $KEY_USE
    0104:   actor PLAYER_ACTOR near_actor MECHA_ACTOR radius 3.0 3.0 1.0 sphere 0
    not actor.Dead(MECHA_ACTOR)
    not actor.Driving(PLAYER_ACTOR)
        then
        if and
        $KANISTRA_USING == 0
        $JIMMY_USING == 0
        not USHER_STORY_FEATRUES_LOCKED == 1
        8A32: not actor PLAYER_ACTOR on_turret_of_car
        0364:   actor MECHA_ACTOR spotted_actor PLAYER_ACTOR
            then
            :MECHA_START_MENU                        
            player.CanMove(PLAYER_CHAR) = false            
            text.RemoveTextBox()
            $PHRASES_OFF_FLAG = 1    
            fade 0 400
            $HUD_REMOVE = 1
            wait 400                        
            text.ClearAll()    
            0470: 22@ = actor PLAYER_ACTOR current_weapon
            06AB: set_actor MECHA_ACTOR all_weapons_hidden 1
            06AB: set_actor PLAYER_ACTOR all_weapons_hidden 1
            01B9: set_actor PLAYER_ACTOR armed_weapon_to 0    
            
            if and
            $FP_ACTIVATED == 1
            CURRENT_USHER_TUTORIAL_NUM == 5
            then
            
            FP_TURNED_OFF_FOR_CUTSCENE = 1
            
            repeat
            wait 0
            until $FP_ACTIVATED == 0
            
            wait 250
            
            end
            
            if and
            $FP_ACTIVATED == 1
            $GAMEMODE_STORYLINE == 1
            then
            repeat
            wait 0
            until $FP_ACTIVATED == 0
            end      
                
            04C4: store_coords_to $TEMPVAR_FLOAT_1 $TEMPVAR_FLOAT_2 $TEMPVAR_FLOAT_3 from_actor PLAYER_ACTOR with_offset 0.5 -1.3 1.0
            015F: set_camera_position $TEMPVAR_FLOAT_1 $TEMPVAR_FLOAT_2 $TEMPVAR_FLOAT_3 rotation 0.0 0.0 0.0
            camera.OnPed(MECHA_ACTOR,15,2)
            0581: enable_radar 0
            cinema.Widescreen(1)
            $PANEL_OPEN = 1
            $PHRASES_OFF_FLAG = 1
            text.ClearAll()
            wait 1
            0639: AS_actor PLAYER_ACTOR rotate_to_actor MECHA_ACTOR
            0639: AS_actor MECHA_ACTOR rotate_to_actor PLAYER_ACTOR
            05BF: AS_actor PLAYER_ACTOR look_at_actor MECHA_ACTOR 999999 ms
            05BF: AS_actor MECHA_ACTOR look_at_actor PLAYER_ACTOR 999999 ms
            wait 400
            fade 1 400              
            wait 400
            $FIST_HUD_ALWAYS = 1
            
            if
            CURRENT_USHER_TUTORIAL_NUM == 5
            then
            
            05C0: AS_actor MECHA_ACTOR look_at_car $USED_CAR[LATEST_CAR_SLOT_USED] 99999 ms
            
            ///////////////////////////////////////////////////////////////////////////////////////////            
                        
            09FD: get_gxt_string 'STRY150' width_to 29@
            0093: 29@ = integer 29@ to_float
            29@ *= 8.0
            0092: 29@ = float 29@ to_integer              
            
            32@ = 0
                
            repeat
            wait 0  
                                                                            
            0384: show_text_1string GXT 'STRY150' string v$2NAME3 time 99 1
                                
            if or
            $SAVESTATUS_TRUE == 1
            $SAVESTATUS == 1
                then
                break
                end
                                
            until 002D:   32@ >= 29@  // (int)            
            
            ///////////////////////////////////////////////////////////////////////////////////////////
            
            0615: define_AS_pack_begin 0@
            05D3: AS_actor -1 goto_point 2152.154053 -2285.053955 14.773438 mode 4 time 10000 ms // versionA
            0616: define_AS_pack_end 0@
            0618: assign_actor MECHA_ACTOR to_AS_pack 0@
            061B: remove_references_to_AS_pack 0@
            
            repeat
            wait 0
            until 02A0:   actor MECHA_ACTOR stopped
            
            0657: car $USED_CAR[LATEST_CAR_SLOT_USED] open_door 0
            wait 500
            05C8: AS_actor MECHA_ACTOR look_around
            wait 3000
            08A6: set_car $USED_CAR[LATEST_CAR_SLOT_USED] door 0 rotation_to 0.0
            0687: clear_actor MECHA_ACTOR task
            0647: AS_actor MECHA_ACTOR clear_look_task
            0A1D: AS_actor MECHA_ACTOR rotate_to_and_look_at_actor PLAYER_ACTOR 
            
            ///////////////////////////////////////////////////////////////////////////////////////////            
                        
            09FD: get_gxt_string 'STRY151' width_to 29@
            0093: 29@ = integer 29@ to_float
            29@ *= 8.0
            0092: 29@ = float 29@ to_integer              
            
            32@ = 0
                
            repeat
            wait 0  
                                                                            
            0384: show_text_1string GXT 'STRY151' string v$2NAME3 time 99 1
                                
            if or
            $SAVESTATUS_TRUE == 1
            $SAVESTATUS == 1
                then
                break
                end
                                
            until 002D:   32@ >= 29@  // (int)
                        
            ///////////////////////////////////////////////////////////////////////////////////////////
            
            fade 0 250
            wait 250
            
            actor.PutAtNoOffset(MECHA_ACTOR,2156.551, -2287.2419, 14.7734)
            actor.Angle(MECHA_ACTOR) = 135.9437
            actor.PutAtNoOffset(PLAYER_ACTOR,2155.5396, -2283.9863, 14.7734)
            actor.Angle(PLAYER_ACTOR) = 188.8735            
            0605: actor MECHA_ACTOR perform_animation "BOM_Plant" IFP "CAMERA" framedelta 4.0 loop 0 lockX 0 lockY 0 lockF 0 time -1                           
            
            repeat
            
            if
            UNITED_MAP == 1
            then
            12@ = OFFROAD_WHEEL_ID_UNITED
            else
            12@ = OFFROAD_WHEEL_ID_SA
            end
            
            06E9: load_car_component 12@
            mload
            wait 1
            
            until 06EA:   car_component 12@ available
                            
            06E7: 0@ = add_car_component 12@ to_car $USED_CAR[LATEST_CAR_SLOT_USED]
            06EB: release_car_component 12@                        
            
            04C4: store_coords_to $TEMPVAR_FLOAT_1 $TEMPVAR_FLOAT_2 $TEMPVAR_FLOAT_3 from_actor PLAYER_ACTOR with_offset 0.5 -1.3 1.0
            015F: set_camera_position $TEMPVAR_FLOAT_1 $TEMPVAR_FLOAT_2 $TEMPVAR_FLOAT_3 rotation 0.0 0.0 0.0
            camera.OnPed(MECHA_ACTOR,15,2)
            
            fade 1 250
            wait 250
            
            ///////////////////////////////////////////////////////////////////////////////////////////            
                        
            09FD: get_gxt_string 'STRY152' width_to 29@
            0093: 29@ = integer 29@ to_float
            29@ *= 8.0
            0092: 29@ = float 29@ to_integer              
            
            32@ = 0
                
            repeat
            wait 0  
                                                                            
            0384: show_text_1string GXT 'STRY152' string v$2NAME3 time 99 1
                                
            if or
            $SAVESTATUS_TRUE == 1
            $SAVESTATUS == 1
                then
                break
                end
                                
            until 002D:   32@ >= 29@  // (int)
            
            0615: define_AS_pack_begin 0@
            05D3: AS_actor -1 goto_point 2153.3491 -2283.8848 14.7734 mode 4 time 10000 ms // versionA
            05D3: AS_actor -1 goto_point 2151.3843 -2283.9429 14.7734 mode 4 time 10000 ms // versionA
            05D4: AS_actor -1 rotate_angle 228.0
            0616: define_AS_pack_end 0@
            0618: assign_actor MECHA_ACTOR to_AS_pack 0@
            061B: remove_references_to_AS_pack 0@

            CURRENT_USHER_TUTORIAL_NUM = 0
                        
            ///////////////////////////////////////////////////////////////////////////////////////////
            
            jump to @MECHANIC_BYE
            
            ///////////////////////////////////////////////////////////////////////////////////////////  
            
            end
            
            :MECHA_CAR_SELECT
            0@ = 0
            gosub @MECHA_SELECT_CAR_MENU_START2            
            if
            0@ == 1
            then                
            gosub @MECHANIC_START_MENU
                        
            if
            $FREEMODE == 1
            then
            jump to @MECHA_CAR_SELECT
            end
                        
            end                        
            :MECHANIC_BYE                        
            $FIST_HUD_ALWAYS = 0
            fade 0 400
            $HUD_REMOVE = 1
            wait 400
            text.ClearAll()
            cinema.Widescreen(0)
            wait 1 
            05BF: AS_actor PLAYER_ACTOR look_at_actor MECHA_ACTOR 1 ms
            05BF: AS_actor MECHA_ACTOR look_at_actor PLAYER_ACTOR 1 ms
            06AB: set_actor PLAYER_ACTOR all_weapons_hidden 0
            06AB: set_actor MECHA_ACTOR all_weapons_hidden 0
            if
            0491:   actor PLAYER_ACTOR has_weapon 22@
            then
            01B9: set_actor PLAYER_ACTOR armed_weapon_to 22@
            end 
            camera.SetBehindPlayer()
            camera.Restore_WithJumpCut()
            
            if
            FP_TURNED_OFF_FOR_CUTSCENE == 1
            then
            FP_TURNED_OFF_FOR_CUTSCENE = 0
            end
            
            fade 1 400
            $HUD_REMOVE = 0  
            wait 400
            cinema.Radar(1)
            $PANEL_OPEN = 0
            player.CanMove(PLAYER_CHAR) = true
            05C1: AS_actor MECHA_ACTOR speak_from_audio_table 162
            
            if
            not actor.Model(MECHA_ACTOR) == 9204   
            then 
            gosub @RANDOM_DIALOG_MECHA_BYE            
            v$2NAME3 = "GAME554"
            else
            gosub @RANDOM_DIALOG_MECHA_BYE_BREYER            
            v$2NAME3 = "N_9204"
            end
                        
            09FD: get_gxt_string v$PHRASE3 width_to 29@
            0093: 29@ = integer 29@ to_float
            29@ *= 8.0
            0092: 29@ = float 29@ to_integer              
            
            32@ = 0
                
            repeat
            wait 0  
                                                                            
            0384: show_text_1string GXT v$PHRASE3 string v$2NAME3 time 99 1
                                
            if or
            $SAVESTATUS_TRUE == 1
            $SAVESTATUS == 1
                then
                break
                end
                                
            until 002D:   32@ >= 29@  // (int)
            
            $PHRASES_OFF_FLAG = 0
                  
            end
        end
    end
else
if
056D:   actor MECHA_ACTOR defined
    then   
    
    if and
    MECHA_AGRED_FLAG == 0
    ZONE_SIREN_FLAG == 1
    then                               
    077A: set_actor MECHA_ACTOR acquaintance 4 to_actors_pedtype 0 // see ped.dat
    077A: set_actor MECHA_ACTOR acquaintance 4 to_actors_pedtype 1 // see ped.dat
    0350: set_actor MECHA_ACTOR maintain_position_when_attacked 0
    05E2: AS_actor MECHA_ACTOR kill_actor PLAYER_ACTOR
    MECHA_AGRED_FLAG = 1
    end
    
    if and
    MECHA_AGRED_FLAG == 0
    MCS_FLAG == 1
    then
    if and
    not $ENTERED_BCAMP_NUMBER == 1
    not $ENTERED_BCAMP_NUMBER == 2
    not $ENTERED_BCAMP_NUMBER == 3
    not $ENTERED_BCAMP_NUMBER == 4
        then
        077A: set_actor MECHA_ACTOR acquaintance 4 to_actors_pedtype 0 // see ped.dat
        077A: set_actor MECHA_ACTOR acquaintance 4 to_actors_pedtype 1 // see ped.dat
        0350: set_actor MECHA_ACTOR maintain_position_when_attacked 0
        05E2: AS_actor MECHA_ACTOR kill_actor PLAYER_ACTOR
        MECHA_AGRED_FLAG = 1
        end
    end 
                 
    if and
    BCAMP_AGRED_FLAG == 0 
    not ZONE_SIREN_FLAG == 1
    $GAMEMODE_WAR == 0   
    $PANEL_OPEN == 0
    0AB0:   key_pressed $KEY_USE
    0104:   actor PLAYER_ACTOR near_actor MECHA_ACTOR radius 3.0 3.0 1.0 sphere 0
    not actor.Dead(MECHA_ACTOR)
    not actor.Driving(PLAYER_ACTOR)
        then
        if and
        $KANISTRA_USING == 0
        $JIMMY_USING == 0
        8A32: not actor PLAYER_ACTOR on_turret_of_car
            then
            jump to @MECHA_START_MENU
            end
        end
    end
end        

end 

end_thread

:MECHANIC_START_MENU

text.ClearAll()

:MECHANIC_START2_MENU

$PANEL_OPEN = 1

$PLAYER_MONEY = player.Money(PLAYER_CHAR)
if
$PLAYER_MONEY > PLAYER_MAX_MONEY_4999
then
$PLAYER_MONEY = PLAYER_MAX_MONEY_5000
end

$PHRASES_OFF_FLAG = 1

//v$2NAME1 = "N_ME"
//0665: get_actor MECHA_ACTOR model_to 0@
//if
//0@ > 10000
//then
//v$2NAME1 = "N_"
//end
//0AD3: v$2NAME2 = format "%d" 0@
//098B: v$2NAME3 = v$2NAME1 + v$2NAME2
0ADB: v$2NAME3 = car_model $SAVE_CAR_MODEL[MECHA_CAR_NUM] name

/////////////////////////////////////////////////////////////////

MECHA_ARMOR_PRICE = 1850
MECHA_WHEEL_PRICE = 250
MECHA_ENGINE_PRICE = 1000
MECHA_REPLACE_WHEEL_PRICE = 150
MECHA_REPAIR_PRICE = 250
MECHA_HYDRA_PRICE = 350
MECHA_PAINT_PRICE = 100
MECHA_CAR_SELL_PRICE = 0

0089: 2@ = $ADDITIONAL_PRICE_COEFF // (float)
0089: 1@ = AMMO_MECHA_ADDITIONAL_PRICE_COEFF // (float)
005B: 2@ += 1@  // (float)

0093: 1@ = integer MECHA_ARMOR_PRICE to_float
006B: 1@ *= 2@  // (float)
0092: 1@ = float 1@ to_integer
005A: MECHA_ARMOR_PRICE += 1@  // (int)

0093: 1@ = integer MECHA_WHEEL_PRICE to_float
006B: 1@ *= 2@  // (float)
0092: 1@ = float 1@ to_integer
005A: MECHA_WHEEL_PRICE += 1@  // (int)

0093: 1@ = integer MECHA_REPLACE_WHEEL_PRICE to_float
006B: 1@ *= 2@  // (float)
0092: 1@ = float 1@ to_integer
005A: MECHA_REPLACE_WHEEL_PRICE += 1@  // (int)

0093: 1@ = integer MECHA_REPAIR_PRICE to_float
006B: 1@ *= 2@  // (float)
0092: 1@ = float 1@ to_integer
005A: MECHA_REPAIR_PRICE += 1@  // (int)

0093: 1@ = integer MECHA_HYDRA_PRICE to_float
006B: 1@ *= 2@  // (float)
0092: 1@ = float 1@ to_integer
005A: MECHA_HYDRA_PRICE += 1@  // (int)

0093: 1@ = integer MECHA_PAINT_PRICE to_float
006B: 1@ *= 2@  // (float)
0092: 1@ = float 1@ to_integer
005A: MECHA_PAINT_PRICE += 1@  // (int)

/////////////////////////////////////////////////////////////////

008B: 1@ = $ENTERED_CAMP_NUMBER // (int)
1@ += 156
if
$RNDM_HOUSE_BANKA[1@] >= 100
then
2@ = -0.15

0093: 1@ = integer MECHA_ARMOR_PRICE to_float
006B: 1@ *= 2@  // (float)
0092: 1@ = float 1@ to_integer
005A: MECHA_ARMOR_PRICE += 1@  // (int)

0093: 1@ = integer MECHA_WHEEL_PRICE to_float
006B: 1@ *= 2@  // (float)
0092: 1@ = float 1@ to_integer
005A: MECHA_WHEEL_PRICE += 1@  // (int)

0093: 1@ = integer MECHA_REPLACE_WHEEL_PRICE to_float
006B: 1@ *= 2@  // (float)
0092: 1@ = float 1@ to_integer
005A: MECHA_REPLACE_WHEEL_PRICE += 1@  // (int)

0093: 1@ = integer MECHA_REPAIR_PRICE to_float
006B: 1@ *= 2@  // (float)
0092: 1@ = float 1@ to_integer
005A: MECHA_REPAIR_PRICE += 1@  // (int)

0093: 1@ = integer MECHA_HYDRA_PRICE to_float
006B: 1@ *= 2@  // (float)
0092: 1@ = float 1@ to_integer
005A: MECHA_HYDRA_PRICE += 1@  // (int)

0093: 1@ = integer MECHA_PAINT_PRICE to_float
006B: 1@ *= 2@  // (float)
0092: 1@ = float 1@ to_integer
005A: MECHA_PAINT_PRICE += 1@  // (int)

end

/////////////////////////////////////////////////////////////////

008B: 1@ = $ENTERED_CAMP_NUMBER // (int)
1@ += 156
if and
USHER_STORY_FLAG == 1
$ENTERED_CAMP_NUMBER == 6
then
2@ = -0.5

0093: 1@ = integer MECHA_ARMOR_PRICE to_float
006B: 1@ *= 2@  // (float)
0092: 1@ = float 1@ to_integer
005A: MECHA_ARMOR_PRICE += 1@  // (int)

0093: 1@ = integer MECHA_WHEEL_PRICE to_float
006B: 1@ *= 2@  // (float)
0092: 1@ = float 1@ to_integer
005A: MECHA_WHEEL_PRICE += 1@  // (int)

0093: 1@ = integer MECHA_REPLACE_WHEEL_PRICE to_float
006B: 1@ *= 2@  // (float)
0092: 1@ = float 1@ to_integer
005A: MECHA_REPLACE_WHEEL_PRICE += 1@  // (int)

0093: 1@ = integer MECHA_REPAIR_PRICE to_float
006B: 1@ *= 2@  // (float)
0092: 1@ = float 1@ to_integer
005A: MECHA_REPAIR_PRICE += 1@  // (int)

0093: 1@ = integer MECHA_HYDRA_PRICE to_float
006B: 1@ *= 2@  // (float)
0092: 1@ = float 1@ to_integer
005A: MECHA_HYDRA_PRICE += 1@  // (int)

0093: 1@ = integer MECHA_PAINT_PRICE to_float
006B: 1@ *= 2@  // (float)
0092: 1@ = float 1@ to_integer
005A: MECHA_PAINT_PRICE += 1@  // (int)

end

/////////////////////////////////////////////////////////////////

if
$MONTHS_PASSED > 0
then

for 0@ = 1 to $MONTHS_PASSED
MECHA_ARMOR_PRICE += 1385
MECHA_WHEEL_PRICE += 185
MECHA_REPLACE_WHEEL_PRICE += 110
MECHA_REPAIR_PRICE += 185
MECHA_HYDRA_PRICE += 260
MECHA_PAINT_PRICE += 75
end

end

/////////////////////////////////////////////////////////////////

MECHA_ENGINE_PRICE = 1001

if
056E:   car $USED_CAR[MECHA_CAR_NUM] defined
then

0A97: 22@ = car $USED_CAR[MECHA_CAR_NUM] struct 
0A8E: 23@ = 22@ + CAR_ENGINE_POWER_PLUS_STRUCT // int 
0A8D: 0@ = read_memory 23@ size 4 virtual_protect 1

if
UNITED_MAP == 0
then

if or
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15405
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15406
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15407
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15408
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15409
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15410
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15411
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15412
then
0@ = 1.3
end

if or
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15413
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15414
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15415
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15416
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15417
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15418
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15419
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15427
then
0@ = 1.3
end

if or
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15428
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15429
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15430
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15431
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15432
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15433
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15434
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15426
then
0@ = 1.3
end

if or
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15465
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15466
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15467
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15468
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15484
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15485
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15488
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15490
then
0@ = 1.3
end

if or
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15495
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15496
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15497
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15498
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15499
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15500
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15501
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15592
then
0@ = 1.3
end

if
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15594
then
0@ = 1.3
end

if or
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15502
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15503
then
0@ = 1.3
end

if or
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15434
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15405
then
MECHA_CAR_SELL_PRICE = 0
end

end

if
0@ < 0.325
    then
    MECHA_ENGINE_PRICE = 1000
    gosub @ENGINE_PRICE_SET
    end
    
if and
0@ < 0.65
0@ > 0.324
    then
    MECHA_ENGINE_PRICE = 1750
    MECHA_CAR_SELL_PRICE += 500
    gosub @ENGINE_PRICE_SET
    end
    
if and
0@ < 0.975
0@ > 0.64
    then
    MECHA_ENGINE_PRICE = 3000
    MECHA_CAR_SELL_PRICE += 1375
    gosub @ENGINE_PRICE_SET
    end
    
if and
0@ < 1.3
0@ > 0.974
    then
    MECHA_ENGINE_PRICE = 3750
    MECHA_CAR_SELL_PRICE += 2875
    gosub @ENGINE_PRICE_SET
    end
    
if
0@ > 1.29
    then
    MECHA_CAR_SELL_PRICE += 4750
    end                

end

0089: 2@ = $ADDITIONAL_PRICE_COEFF // (float)
0093: 1@ = integer MECHA_ENGINE_PRICE to_float
006B: 1@ *= 2@  // (float)
0092: 1@ = float 1@ to_integer
005A: MECHA_ENGINE_PRICE += 1@  // (int)

0093: 1@ = integer MECHA_CAR_SELL_PRICE to_float
006B: 1@ *= 2@  // (float)
0092: 1@ = float 1@ to_integer
005A: MECHA_CAR_SELL_PRICE += 1@  // (int)

if
056E:   car $USED_CAR[MECHA_CAR_NUM] defined
then

if
0803:   car $USED_CAR[MECHA_CAR_NUM] have_hydraulics
then
MECHA_CAR_SELL_PRICE += 250
end

end

096D: get_car $USED_CAR[MECHA_CAR_NUM] component_on_slot 12 model_to 1@
if or
1@ == OFFROAD_WHEEL_ID_SA
1@ == OFFROAD_WHEEL_ID_UNITED
then
MECHA_CAR_SELL_PRICE += 125
end

if
0185:   car $USED_CAR[MECHA_CAR_NUM] health >= 1005
then
1@ = car.Health($USED_CAR[MECHA_CAR_NUM])
1@ -= 1000
0093: 1@ = integer 1@ to_float
1@ *= 0.37
0092: 1@ = float 1@ to_integer

if
1@ > 0
then
005A: MECHA_CAR_SELL_PRICE += 1@  // (int)
end

else

1@ = car.Health($USED_CAR[MECHA_CAR_NUM])
1@ -= 250
0093: 1@ = integer 1@ to_float

2@ = car.Model($USED_CAR[MECHA_CAR_NUM])
09E1: get_car_model 2@ price_to 3@

//0AD1: show_formatted_text_highpriority "price is %d" time 2000 3@
//wait 2000
0093: 3@ = integer 3@ to_float 

//////////////////////////////////////////////////////////////////////////////

if or
2@ == 539
2@ == 15594
then
2@ = 400
end

if and
8A01: not  model 2@ car
8820: not  model 2@ heli
881E: not  model 2@ boat
881F: not  model 2@ plane
    then
    jump to @SELL_PRICE_DAMAGED_PARTS_COUNT_SKIP_JUMP
    end

if or
0A01:   model 2@ car
2@ == 406
2@ == 444
2@ == 556
2@ == 557
2@ == 573
2@ == 15496 
2@ == 15497  
    then   
    
    if
    09BB:   car $USED_CAR[MECHA_CAR_NUM] has_visible_damage_on_component 0
        then        
        3@ *= 0.9
        end
     
    if
    09BB:   car $USED_CAR[MECHA_CAR_NUM] has_visible_damage_on_component 1
        then
        3@ *= 0.9
        end
    
    if
    09BB:   car $USED_CAR[MECHA_CAR_NUM] has_visible_damage_on_component 2
        then    
        095F: get_car $USED_CAR[MECHA_CAR_NUM] door 2 angle_to 4@
        if
        4@ >= 0.0
            then
            3@ *= 0.9      
            end    
        end
    
    if
    09BB:   car $USED_CAR[MECHA_CAR_NUM] has_visible_damage_on_component 3
        then
        095F: get_car $USED_CAR[MECHA_CAR_NUM] door 3 angle_to 4@
        if
        4@ >= 0.0
            then
            3@ *= 0.9
            end
        end
        
    if
    8D0D: not 4@ = get_car $USED_CAR[MECHA_CAR_NUM] component "door_lr_dummy" // IF and SET 
        then
        jump to @SELL_PRICE_DAMAGED_PARTS_COUNT_SKIP_JUMP2
        end    
    
    if
    09BB:   car $USED_CAR[MECHA_CAR_NUM] has_visible_damage_on_component 4
        then
        095F: get_car $USED_CAR[MECHA_CAR_NUM] door 4 angle_to 4@
        if
        4@ >= 0.0
            then
            3@ *= 0.9
            end
        end
    
    if
    09BB:   car $USED_CAR[MECHA_CAR_NUM] has_visible_damage_on_component 5
        then
        095F: get_car $USED_CAR[MECHA_CAR_NUM] door 5 angle_to 4@
        if
        4@ >= 0.0
            then
            3@ *= 0.9
            end
        end
        
    :SELL_PRICE_DAMAGED_PARTS_COUNT_SKIP_JUMP2
        
    if
    03C9:   car $USED_CAR[MECHA_CAR_NUM] damaged
    then
    3@ *= 0.5
    end
    
    else
    jump to @SELL_PRICE_DAMAGED_PARTS_COUNT_SKIP_JUMP
    end 
    
////////////////////////////////////////////////////////////////////////////// 

:SELL_PRICE_DAMAGED_PARTS_COUNT_SKIP_JUMP

3@ /= 30000.0
//0AD1: show_formatted_text_highpriority "nacenka is %f" time 2000 3@
//wait 2000
1@ *= 0.55
006B: 1@ *= 3@  // (float)
0092: 1@ = float 1@ to_integer

if
1@ > 0
then
005A: MECHA_CAR_SELL_PRICE += 1@  // (int)
end

end

/////////////////////////////////////////////////////////////////

0512: show_permanent_text_box 'GAME545'
$PANEL = 0
08D4: $PANEL = create_panel_with_title 'GME1452' position $X_MENU_SELECT_GAMEMODE $Y_MENU_SELECT_GAMEMODE width $WIDTH_MENU_SELECT_GAMEMODE columns 2 interactive 1 background 1 alignment 1

1@ = car.Model($USED_CAR[MECHA_CAR_NUM])
if or
1@ == 539
1@ == 15594
then
1@ = 400
end

if or
0A01:   model 1@ car
1@ == 406
1@ == 444
1@ == 556
1@ == 557
1@ == 573
1@ == 15496
1@ == 15497                
then  
08DB: set_panel $PANEL column 0 header 'GAME546' data 'GAME547' 'GAME548' 'GAME549' 'GAME550' 'GAME551' 'GAME552' 'GAME555' 'INV294' 'INV15' 'INV153' 'DUMMY' 'DUMMY'
else
MECHA_ARMOR_PRICE /= 2
0093: 1@ = integer MECHA_ENGINE_PRICE to_float
1@ *= 0.6
0092: MECHA_ENGINE_PRICE = float 1@ to_integer
08DB: set_panel $PANEL column 0 header 'GAME546' data 'GAME547' 'INV359' 'GAME549' 'GAME550' 'GAME551' 'GAME552' 'GAME555' 'INV294' 'INV15' 'INV153' 'DUMMY' 'DUMMY'
end

08D6: set_panel $PANEL column 1 alignment 1
08EE: set_panel $PANEL column 1 row 0 text_2numbers GXT 'TRDR58' number MECHA_ARMOR_PRICE
08EE: set_panel $PANEL column 1 row 1 text_2numbers GXT 'TRDR58' number MECHA_WHEEL_PRICE
if
not MECHA_ENGINE_PRICE == 1001
then
08EE: set_panel $PANEL column 1 row 2 text_2numbers GXT 'TRDR58' number MECHA_ENGINE_PRICE
else
08EE: set_panel $PANEL column 1 row 2 text_2numbers GXT 'TRDR122' number MECHA_ENGINE_PRICE
end
08EE: set_panel $PANEL column 1 row 3 text_2numbers GXT 'TRDR58' number MECHA_REPLACE_WHEEL_PRICE
08EE: set_panel $PANEL column 1 row 4 text_2numbers GXT 'TRDR58' number MECHA_REPAIR_PRICE
08EE: set_panel $PANEL column 1 row 5 text_2numbers GXT 'TRDR58' number MECHA_HYDRA_PRICE
08EE: set_panel $PANEL column 1 row 6 text_2numbers GXT 'TRDR58' number MECHA_PAINT_PRICE
08EE: set_panel $PANEL column 1 row 7 text_2numbers GXT 'TRDR58' number MECHA_CAR_SELL_PRICE
08EE: set_panel $PANEL column 1 row 9 text_1number GXT 'INV3' number $PLAYER_MONEY
09DB: set_panel $PANEL column 0 width 130 
09DB: set_panel $PANEL column 1 width 50
08D9: set_panel $PANEL row 8 enable 0
08D9: set_panel $PANEL row 9 enable 0
090E: set_panel $PANEL active_row $PANEL_MC_ACTIVE_ROW
if
0@ > 1.29
then
08D9: set_panel $PANEL row 2 enable 0
end

if or
MECHA_CAR_SELL_PRICE < 1
USHER_STORY_FLAG == 1
then
08D9: set_panel $PANEL row 7 enable 0
end                   

wait 100

:MECHANIC_MENU_IDLE
wait 0

$PHRASES_OFF_FLAG = 1

if
8A2A: not  text_box 'GAME545' displayed
then
0512: show_permanent_text_box 'GAME545' 
end      

08D7: $PANEL_MC_ACTIVE_ROW = panel $PANEL active_row        

if
$PANEL_MC_ACTIVE_ROW == 0 
    then       
    if
    00E1: player 0 pressed_key 16
        then
        if
        056E:   car $USED_CAR[MECHA_CAR_NUM] defined
            then
            $PLAYER_MONEY = player.Money(PLAYER_CHAR)
            if
            002C:   $PLAYER_MONEY >= MECHA_ARMOR_PRICE  // (int)
                then                                                                       
                0@ = car.Model($USED_CAR[MECHA_CAR_NUM])
                if or
                0@ == 539
                0@ == 15594
                then
                0@ = 400
                end
                
                if and
                8A01: not  model 0@ car
                8820: not  model 0@ heli
                881E: not  model 0@ boat
                881F: not  model 0@ plane
                    then
                    jump to @BIKE_ARMOUR_SKIP_JUMP
                    end
                
                if or
                0A01:   model 0@ car
                0@ == 406
                0@ == 444
                0@ == 556
                0@ == 557
                0@ == 573
                0@ == 15496 
                0@ == 15497               
                    then
                    :BIKE_ARMOUR_SKIP_JUMP
                    if
                    8185: not  car $USED_CAR[MECHA_CAR_NUM] health >= 2499                
                        then    
                        03E6: remove_text_box 
                        08DA: remove_panel $PANEL  
                        00BE: text_clear_all
                        text.ShowH('INV273',2000,1)
                        actor.StorePos(PLAYER_ACTOR,0@,1@,2@)
                        097A: play_audio_at 0@ 1@ 2@ event 1133
                        if
                        8185: not  car $USED_CAR[MECHA_CAR_NUM] health >= 999
                        then
                        0A30: repair_car $USED_CAR[MECHA_CAR_NUM]
                        end                    
                        0227: 1@ = car $USED_CAR[MECHA_CAR_NUM] health
                        
                        0@ = car.Model($USED_CAR[MECHA_CAR_NUM])
                        if or
                        0@ == 539
                        0@ == 15594
                        then
                        0@ = 400
                        end
                        
                        if and
                        8A01: not  model 0@ car
                        8820: not  model 0@ heli
                        881E: not  model 0@ boat
                        881F: not  model 0@ plane
                        then                        
                        if or
                        0A01:   model 0@ car
                        0@ == 406
                        0@ == 444
                        0@ == 556
                        0@ == 557
                        0@ == 573
                        0@ == 15496
                        0@ == 15497                
                            then
                            1@ += 5000
                            else
                            1@ += 2500
                            end                        
                        else
                        1@ += 5000
                        end
                                                
                        car.Health($USED_CAR[MECHA_CAR_NUM]) = 1@
                        020A: set_car $USED_CAR[MECHA_CAR_NUM] door_status_to 6
                        0852: set_car $USED_CAR[MECHA_CAR_NUM] damages_visible 0
                        03AB: set_car $USED_CAR[MECHA_CAR_NUM] strong 1
                        053F: set_car $USED_CAR[MECHA_CAR_NUM] tires_vulnerability 0
                        ///////////////////////                        
                        0A97: 0@ = car $USED_CAR[MECHA_CAR_NUM] struct 
                        0@ += 0x8C 
                        0A8D: 1@ = read_memory 0@ size 4 virtual_protect 1
                        1@ *= 1.25
                        0A8C: write_memory 0@ size 4 value 1@ virtual_protect 1
                        ///////////////////////
                        0A97: 0@ = car $USED_CAR[MECHA_CAR_NUM] struct 
                        0@ += 0x90 
                        0A8D: 1@ = read_memory 0@ size 4 virtual_protect 1
                        1@ *= 1.45
                        0A8C: write_memory 0@ size 4 value 1@ virtual_protect 1
                        /////////////////////////                        
                        MECHA_ARMOR_PRICE *= -2
                        MECHA_ARMOR_PRICE /= 2
                        Player.Money(PLAYER_CHAR) += MECHA_ARMOR_PRICE
                        MECHA_ARMOR_PRICE *= -2
                        MECHA_ARMOR_PRICE /= 2
                        jump to @MECHANIC_START2_MENU
                        else
                        00BC: show_text_highpriority GXT 'INV274' time 2000 flag 1
                        05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
                        end
                    else
                    00BC: show_text_highpriority GXT 'TRDR119' time 2000 flag 1
                    05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
                    end
                else
                00BC: show_text_highpriority GXT 'TRDR26' time 2000 flag 1
                05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161    
                end                
            else
            00BC: show_text_highpriority GXT 'INV271' time 2000 flag 1
            05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
            end
        end
    end
    
if
$PANEL_MC_ACTIVE_ROW == 1 
    then       
    if
    00E1: player 0 pressed_key 16
        then
        if
        056E:   car $USED_CAR[MECHA_CAR_NUM] defined
            then
            $PLAYER_MONEY = player.Money(PLAYER_CHAR)
            if
            002C:   $PLAYER_MONEY >= MECHA_WHEEL_PRICE  // (int)
                then
                0@ = car.Model($USED_CAR[MECHA_CAR_NUM])
                if and
                0A01:   model 0@ car
                8D0C: not get_car $USED_CAR[MECHA_CAR_NUM] component "wheel_lm_dummy" matrix_pointer_to 1@ // IF and SET 
                    then
                    096D: get_car $USED_CAR[MECHA_CAR_NUM] component_on_slot 12 model_to 0@
                    if and
                    not 0@ == OFFROAD_WHEEL_ID_SA
                    not 0@ == OFFROAD_WHEEL_ID_UNITED
                        then
                        
                        player.CanMove(PLAYER_CHAR,true)                                                    
                        
                        32@ = 0
                        12@ = OFFROAD_WHEEL_ID_SA                                                                                                                
                        
                        repeat
                        
                        if
                        UNITED_MAP == 1
                        then
                        12@ = OFFROAD_WHEEL_ID_UNITED
                        else
                        12@ = OFFROAD_WHEEL_ID_SA
                        end
                        
                        06E9: load_car_component 12@
                        mload
                        wait 1                                                
                        
                        if
                        32@ > 5000
                        then
                        03E6: remove_text_box 
                        08DA: remove_panel $PANEL  
                        00BE: text_clear_all
                        jump to @WHEEL_SKIP_PLACE
                        end
                                                                                                
                        until 06EA:   car_component 12@ available
                        
                        03E6: remove_text_box 
                        08DA: remove_panel $PANEL  
                        00BE: text_clear_all
                        
                        player.CanMove(PLAYER_CHAR,false)                        
                        
                        0699: set_car $USED_CAR[MECHA_CAR_NUM] repair_tire 0
                        0699: set_car $USED_CAR[MECHA_CAR_NUM] repair_tire 1
                        0699: set_car $USED_CAR[MECHA_CAR_NUM] repair_tire 2
                        0699: set_car $USED_CAR[MECHA_CAR_NUM] repair_tire 3
                                                                
                        06E7: 0@ = add_car_component 12@ to_car $USED_CAR[MECHA_CAR_NUM]
                        06EB: release_car_component 12@
                        
                        text.ShowH('INV273',2000,1)
                        actor.StorePos(PLAYER_ACTOR,0@,1@,2@)
                        097A: play_audio_at 0@ 1@ 2@ event 1133
                        
                        MECHA_WHEEL_PRICE *= -2
                        MECHA_WHEEL_PRICE /= 2
                        Player.Money(PLAYER_CHAR) += MECHA_WHEEL_PRICE
                        MECHA_WHEEL_PRICE *= -2
                        MECHA_WHEEL_PRICE /= 2
                        :WHEEL_SKIP_PLACE
                        jump to @MECHANIC_START2_MENU
                        else
                        00BC: show_text_highpriority GXT 'INV274' time 2000 flag 1
                        05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
                        end
                    else
                    
                    if or
                    0@ == 15497
                    0@ == #KART
                    then
                    0@ = #MONSTER
                    end
                    
                    if or
                    0820:  model 0@ heli
                    081E:  model 0@ boat
                    081F:  model 0@ plane
                    then
                    0@ = 406
                    end
                    
                    if and
                    8A01: not  model 0@ car
                    8D0C: not get_car $USED_CAR[MECHA_CAR_NUM] component "wheel_lm_dummy" matrix_pointer_to 1@ // IF and SET 
                    not 0@ == 406
                    not 0@ == 444
                    not 0@ == 556
                    not 0@ == 557
                    not 0@ == 573
                    not 0@ == 15496
                        then
                        if
                        $CAR_WHEEL_INSTALLED[MECHA_CAR_NUM] == 0
                            then
                            
                            player.CanMove(PLAYER_CHAR,true)
                                
                            03E6: remove_text_box 
                            08DA: remove_panel $PANEL  
                            00BE: text_clear_all                                                
                            
                            player.CanMove(PLAYER_CHAR,false)                        
                                                      
                            $CAR_WHEEL_INSTALLED[MECHA_CAR_NUM] = 2                                        
                            
                            text.ShowH('INV273',2000,1)
                            actor.StorePos(PLAYER_ACTOR,0@,1@,2@)
                            097A: play_audio_at 0@ 1@ 2@ event 1133
                            
                            MECHA_WHEEL_PRICE *= -2
                            MECHA_WHEEL_PRICE /= 2
                            Player.Money(PLAYER_CHAR) += MECHA_WHEEL_PRICE
                            MECHA_WHEEL_PRICE *= -2
                            MECHA_WHEEL_PRICE /= 2
                            jump to @MECHANIC_START2_MENU
                            else
                            00BC: show_text_highpriority GXT 'INV274' time 2000 flag 1
                            05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
                            end
                        else
                        00BC: show_text_highpriority GXT 'INV272' time 2000 flag 1
                        05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
                        end
                    end
                else
                00BC: show_text_highpriority GXT 'TRDR26' time 2000 flag 1
                05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
                end                
            else
            00BC: show_text_highpriority GXT 'INV271' time 2000 flag 1
            05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
            end
        end
    end
    
if
$PANEL_MC_ACTIVE_ROW == 2 
    then       
    if
    00E1: player 0 pressed_key 16
        then
        if
        056E:   car $USED_CAR[MECHA_CAR_NUM] defined
            then
            $PLAYER_MONEY = player.Money(PLAYER_CHAR)
            if
            002C:   $PLAYER_MONEY >= MECHA_ENGINE_PRICE  // (int)
                then
                0@ = car.Model($USED_CAR[MECHA_CAR_NUM])
                if and
                not 0@ == #BIKE
                not 0@ == #MTBIKE
                not 0@ == #BMX
                8820: not  model 0@ heli
                881E: not  model 0@ boat
                881F: not  model 0@ plane
                    then
                    0A97: 22@ = car $USED_CAR[MECHA_CAR_NUM] struct 
                    0A8E: 23@ = 22@ + CAR_ENGINE_POWER_PLUS_STRUCT // int 
                    0A8D: 0@ = read_memory 23@ size 4 virtual_protect 1                                
                    if
                    not 0@ == 1.3
                        then                           
                        03E6: remove_text_box 
                        08DA: remove_panel $PANEL  
                        00BE: text_clear_all
                        text.ShowH('INV273',2000,1)                    
                                            
                        0A97: 22@ = car $USED_CAR[MECHA_CAR_NUM] struct 
                        0A8E: 23@ = 22@ + CAR_ENGINE_POWER_PLUS_STRUCT // int
                        0@ += 0.325 
                        0A8C: write_memory 23@ size 4 value 0@ virtual_protect 1
                        $CAR_ENGINE_POWER[MECHA_CAR_NUM] += 0.325 
                        
                        actor.StorePos(PLAYER_ACTOR,0@,1@,2@)
                        097A: play_audio_at 0@ 1@ 2@ event 1133
                        
                        MECHA_ENGINE_PRICE *= -2
                        MECHA_ENGINE_PRICE /= 2
                        Player.Money(PLAYER_CHAR) += MECHA_ENGINE_PRICE
                        MECHA_ENGINE_PRICE *= -2
                        MECHA_ENGINE_PRICE /= 2
                        jump to @MECHANIC_START2_MENU
                        else
                        00BC: show_text_highpriority GXT 'INV274' time 2000 flag 1
                        05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
                        end
                    else
                    00BC: show_text_highpriority GXT 'INV272' time 2000 flag 1
                    05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
                    end
                else
                00BC: show_text_highpriority GXT 'TRDR26' time 2000 flag 1
                05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
                end                
            else
            00BC: show_text_highpriority GXT 'INV271' time 2000 flag 1
            05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
            end
        end
    end    
    
if
$PANEL_MC_ACTIVE_ROW == 3 
    then       
    if
    00E1: player 0 pressed_key 16
        then
        if
        056E:   car $USED_CAR[MECHA_CAR_NUM] defined
            then
            $PLAYER_MONEY = player.Money(PLAYER_CHAR)
            if
            002C:   $PLAYER_MONEY >= MECHA_REPLACE_WHEEL_PRICE  // (int)
                then
                0@ = car.Model($USED_CAR[MECHA_CAR_NUM])
                
                if and
                8A01: not  model 0@ car
                8820: not  model 0@ heli
                881E: not  model 0@ boat
                881F: not  model 0@ plane
                    then
                    jump to @BIKE_TIRE_SKIP_JUMP
                    end
                
                if and
                not 0@ == #BIKE
                not 0@ == #MTBIKE
                not 0@ == #BMX
                0A01:   model 0@ car
                    then
                    if or
                    0496:   tire 0 on_car $USED_CAR[MECHA_CAR_NUM] deflated
                    0496:   tire 1 on_car $USED_CAR[MECHA_CAR_NUM] deflated
                    0496:   tire 2 on_car $USED_CAR[MECHA_CAR_NUM] deflated
                    0496:   tire 3 on_car $USED_CAR[MECHA_CAR_NUM] deflated
                        then    
                        03E6: remove_text_box 
                        08DA: remove_panel $PANEL  
                        00BE: text_clear_all
                        text.ShowH('INV284',2000,1)
                        0699: set_car $USED_CAR[MECHA_CAR_NUM] repair_tire 0
                        0699: set_car $USED_CAR[MECHA_CAR_NUM] repair_tire 1
                        0699: set_car $USED_CAR[MECHA_CAR_NUM] repair_tire 2
                        0699: set_car $USED_CAR[MECHA_CAR_NUM] repair_tire 3                                            
                                                
                        actor.StorePos(PLAYER_ACTOR,0@,1@,2@)
                        097A: play_audio_at 0@ 1@ 2@ event 1133
                        
                        MECHA_REPLACE_WHEEL_PRICE *= -2
                        MECHA_REPLACE_WHEEL_PRICE /= 2
                        Player.Money(PLAYER_CHAR) += MECHA_REPLACE_WHEEL_PRICE
                        MECHA_REPLACE_WHEEL_PRICE *= -2
                        MECHA_REPLACE_WHEEL_PRICE /= 2
                        jump to @MECHANIC_START2_MENU
                        :BIKE_TIRE_SKIP_JUMP
                        if or
                        0496:   tire 0 on_car $USED_CAR[MECHA_CAR_NUM] deflated
                        0496:   tire 1 on_car $USED_CAR[MECHA_CAR_NUM] deflated
                            then
                            03E6: remove_text_box 
                            08DA: remove_panel $PANEL  
                            00BE: text_clear_all
                            text.ShowH('INV284',2000,1)
                            //это пиздец нахуй
//                            0699: set_car $USED_CAR[MECHA_CAR_NUM] repair_tire 0
//                            0699: set_car $USED_CAR[MECHA_CAR_NUM] repair_tire 1
                            0@ = car.Health($USED_CAR[MECHA_CAR_NUM])
                            0A30: repair_car $USED_CAR[MECHA_CAR_NUM]
                            car.Health($USED_CAR[MECHA_CAR_NUM]) = 0@                                          
                                                    
                            actor.StorePos(PLAYER_ACTOR,0@,1@,2@)
                            097A: play_audio_at 0@ 1@ 2@ event 1133
                            
                            MECHA_REPLACE_WHEEL_PRICE *= -2
                            MECHA_REPLACE_WHEEL_PRICE /= 2
                            Player.Money(PLAYER_CHAR) += MECHA_REPLACE_WHEEL_PRICE
                            MECHA_REPLACE_WHEEL_PRICE *= -2
                            MECHA_REPLACE_WHEEL_PRICE /= 2
                            jump to @MECHANIC_START2_MENU
                            else
                            00BC: show_text_highpriority GXT 'INV285' time 2000 flag 1
                            05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
                            end
                        else
                        00BC: show_text_highpriority GXT 'INV285' time 2000 flag 1
                        05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
                        end
                    else
                    00BC: show_text_highpriority GXT 'INV272' time 2000 flag 1
                    05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
                    end
                else
                00BC: show_text_highpriority GXT 'TRDR26' time 2000 flag 1
                05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
                end                
            else
            00BC: show_text_highpriority GXT 'INV271' time 2000 flag 1
            05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
            end
        end
    end
    
if
$PANEL_MC_ACTIVE_ROW == 4 
    then       
    if
    00E1: player 0 pressed_key 16
        then
        if
        056E:   car $USED_CAR[MECHA_CAR_NUM] defined
            then
            $PLAYER_MONEY = player.Money(PLAYER_CHAR)
            if
            002C:   $PLAYER_MONEY >= MECHA_REPAIR_PRICE  // (int)
                then
                0@ = car.Model($USED_CAR[MECHA_CAR_NUM])
                if and
                not 0@ == #BIKE
                not 0@ == #MTBIKE
                not 0@ == #BMX
                    then
                    if
                    8185: not  car $USED_CAR[MECHA_CAR_NUM] health >= 999
                        then    
                        03E6: remove_text_box 
                        08DA: remove_panel $PANEL  
                        00BE: text_clear_all
                        text.ShowH('INV275',2000,1)                                        
                        
                        actor.StorePos(PLAYER_ACTOR,0@,1@,2@)
                        097A: play_audio_at 0@ 1@ 2@ event 1133
                        
                        0A30: repair_car $USED_CAR[MECHA_CAR_NUM]
                        0508: car $USED_CAR[MECHA_CAR_NUM] close_all_doors
                        
                        MECHA_REPAIR_PRICE *= -2
                        MECHA_REPAIR_PRICE /= 2
                        Player.Money(PLAYER_CHAR) += MECHA_REPAIR_PRICE
                        MECHA_REPAIR_PRICE *= -2
                        MECHA_REPAIR_PRICE /= 2
                        jump to @MECHANIC_START2_MENU
                        else
                        00BC: show_text_highpriority GXT 'INV277' time 2000 flag 1
                        05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
                        end
                    else
                    00BC: show_text_highpriority GXT 'INV272' time 2000 flag 1
                    05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
                    end
                else
                00BC: show_text_highpriority GXT 'TRDR26' time 2000 flag 1
                05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
                end                
            else
            00BC: show_text_highpriority GXT 'INV271' time 2000 flag 1
            05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
            end
        end
    end
    
if
$PANEL_MC_ACTIVE_ROW == 5 
    then       
    if
    00E1: player 0 pressed_key 16
        then
        if
        056E:   car $USED_CAR[MECHA_CAR_NUM] defined
            then
            $PLAYER_MONEY = player.Money(PLAYER_CHAR)
            if
            002C:   $PLAYER_MONEY >= MECHA_HYDRA_PRICE  // (int)
                then
                0@ = car.Model($USED_CAR[MECHA_CAR_NUM])
                if
                0A01:   model 0@ car
                    then
                    if
                    8803: not  car $USED_CAR[MECHA_CAR_NUM] have_hydraulics
                        then    
                        03E6: remove_text_box 
                        08DA: remove_panel $PANEL  
                        00BE: text_clear_all
                        text.ShowH('INV273',2000,1)                                        
                        
                        actor.StorePos(PLAYER_ACTOR,0@,1@,2@)
                        097A: play_audio_at 0@ 1@ 2@ event 1133
                        
                        07FF: set_car $USED_CAR[MECHA_CAR_NUM] hydraulics 1
                        
                        MECHA_HYDRA_PRICE *= -2
                        MECHA_HYDRA_PRICE /= 2
                        Player.Money(PLAYER_CHAR) += MECHA_HYDRA_PRICE
                        MECHA_HYDRA_PRICE *= -2
                        MECHA_HYDRA_PRICE /= 2
                        jump to @MECHANIC_START2_MENU
                        else
                        00BC: show_text_highpriority GXT 'INV274' time 2000 flag 1
                        05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
                        end
                    else
                    00BC: show_text_highpriority GXT 'INV272' time 2000 flag 1
                    05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
                    end
                else
                00BC: show_text_highpriority GXT 'TRDR26' time 2000 flag 1
                05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
                end                
            else
            00BC: show_text_highpriority GXT 'INV271' time 2000 flag 1
            05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
            end
        end
    end
    
if
$PANEL_MC_ACTIVE_ROW == 6 
    then       
    if
    00E1: player 0 pressed_key 16
        then
        if
        056E:   car $USED_CAR[MECHA_CAR_NUM] defined
            then
            gosub @MECHA_PAINT_NOT_AVALIABLE_CHECK
            if
            0@ == 1
                then
                03F3: get_car $USED_CAR[MECHA_CAR_NUM] primary_color_to 10@ secondary_color_to 11@
                $PLAYER_MONEY = player.Money(PLAYER_CHAR)
                if
                002C:   $PLAYER_MONEY >= MECHA_PAINT_PRICE  // (int)
                    then    
                    03E6: remove_text_box 
                    08DA: remove_panel $PANEL  
                    00BE: text_clear_all
                    
                    :MECHA_COLOR_SELECT1
                    0964: create_square_color_panel 'CARM1' position 400.0 20.0 width 25.7 columns 8 interactive 1 background 1 alignment 1 panelID $PANEL
                    0512: show_permanent_text_box 'INV290'
                    wait 100                                
                    
                    repeat
                    wait 0
                    
                    if
                    8A2A: not  text_box 'INV290' displayed
                    then
                    0512: show_permanent_text_box 'INV290' 
                    end
                    
                    08D7: 0@ = panel $PANEL active_row
                    0A22: set_car_color_to_panel_color_panelID $PANEL car $USED_CAR[MECHA_CAR_NUM] colorslot 1 active_row 0@
                    
                    if
                    00E1: player 0 pressed_key 16
                    then
                    1@ = 0
                    break
                    end
                    
                    if
                    00E1: player 0 pressed_key 15
                    then
                    1@ = 1
                    break
                    end
                    
                    until false                            
                    
                    03F3: get_car $USED_CAR[MECHA_CAR_NUM] primary_color_to 3@ secondary_color_to 4@
                    
                    03E6: remove_text_box 
                    08DA: remove_panel $PANEL  
                    00BE: text_clear_all                                
                    
                    if
                    1@ == 0
                    then
                    
                    gosub @MECHA_CAR_COLS_CHECK_SA
                    if
                    not 12@ == 2
                    then
                    jump to @CAR_MECHA_PAINT_SET
                    end
                    
                    end                                
                    
                    if
                    1@ == 0
                        then
                        
                        :MECHA_COLOR_SELECT2
                        0964: create_square_color_panel 'CARM1' position 400.0 20.0 width 25.7 columns 8 interactive 1 background 1 alignment 1 panelID $PANEL
                        0512: show_permanent_text_box 'INV292'
                        wait 100                                
                        
                        repeat
                        wait 0
                        
                        if
                        8A2A: not  text_box 'INV292' displayed
                        then
                        0512: show_permanent_text_box 'INV292' 
                        end
                        
                        08D7: 2@ = panel $PANEL active_row
                        0A22: set_car_color_to_panel_color_panelID $PANEL car $USED_CAR[MECHA_CAR_NUM] colorslot 2 active_row 2@
                        
                        if
                        00E1: player 0 pressed_key 16
                        then
                        1@ = 0
                        break
                        end
                        
                        if
                        00E1: player 0 pressed_key 15
                        then
                        1@ = 1
                        break
                        end
                        
                        until false                                         
                        
                        03E6: remove_text_box 
                        08DA: remove_panel $PANEL  
                        00BE: text_clear_all
                        
                        if
                        1@ == 0
                            then
                        
                            :CAR_MECHA_PAINT_SET
                            0878: set_car $USED_CAR[MECHA_CAR_NUM] dirt_level 0.0
                            
                            text.ShowH('INV276',2000,1)                                        
                                
                            actor.StorePos(PLAYER_ACTOR,0@,1@,2@)
                            097A: play_audio_at 0@ 1@ 2@ event 1134                    
                                
                            MECHA_PAINT_PRICE *= -2
                            MECHA_PAINT_PRICE /= 2
                            Player.Money(PLAYER_CHAR) += MECHA_PAINT_PRICE
                            MECHA_PAINT_PRICE *= -2
                            MECHA_PAINT_PRICE /= 2
                            jump to @MECHANIC_START2_MENU
                            else
                            0229: set_car $USED_CAR[MECHA_CAR_NUM] primary_color_to 10@ secondary_color_to 11@
                            jump to @MECHANIC_START2_MENU
                            end
                        else
                        0229: set_car $USED_CAR[MECHA_CAR_NUM] primary_color_to 10@ secondary_color_to 11@
                        jump to @MECHANIC_START2_MENU
                        end
                    else
                    00BC: show_text_highpriority GXT 'TRDR26' time 2000 flag 1
                    05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
                    end                
                else
                00BC: show_text_highpriority GXT 'INV272' time 2000 flag 1
                05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
                end
            else
            00BC: show_text_highpriority GXT 'INV271' time 2000 flag 1
            05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
            end
        end
    end
    
if
$PANEL_MC_ACTIVE_ROW == 7 
    then       
    if
    00E1: player 0 pressed_key 16
        then
        if
        056E:   car $USED_CAR[MECHA_CAR_NUM] defined
            then
            01E9: 0@ = car $USED_CAR[MECHA_CAR_NUM] num_passengers
            if
            0@ > 0
                then
                00BC: show_text_highpriority GXT 'INV345' time 2000 flag 1
                05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
                else    
                if
                actor.InCar(PLAYER_ACTOR,$USED_CAR[MECHA_CAR_NUM]) 
                    then
                    00BC: show_text_highpriority GXT 'INV345B' time 2000 flag 1
                    05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
                    else           
                    03E6: remove_text_box 
                    08DA: remove_panel $PANEL  
                    00BE: text_clear_all            
                                
                    repeat
                    wait 0
                    
                    text.Show1Number('INV295',MECHA_CAR_SELL_PRICE,10,1)
                    
                    if
                    0E3E: is_button_just_pressed 0 button 11
                        then
                        text.ClearAll()
                        break
                        end
                        
                    if
                    0E3E: is_button_just_pressed 0 button 10
                        then
                        text.ClearAll()
                        jump to @MECHANIC_START2_MENU
                        end    
                    
                    until false
                    
                    text.Show1Number('INV296',MECHA_CAR_SELL_PRICE,3100,1)                                                                                        
                    Player.Money(PLAYER_CHAR) += MECHA_CAR_SELL_PRICE
                    
                    if
                    075C:   marker $CAR_MARKER[MECHA_CAR_NUM] enabled
                    then
                    marker.Disable($CAR_MARKER[MECHA_CAR_NUM])
                    $CAR_MARKER[MECHA_CAR_NUM] = 0
                    end
                                
                    $CAR_MARKER_CREATED[MECHA_CAR_NUM] = 0 
                    $CAR_ENTERED[MECHA_CAR_NUM] = 0   
                    $CAR_HINT_FLAG[MECHA_CAR_NUM] = 0
                    $SAVE_CAR_MODEL[MECHA_CAR_NUM] = -1
                    $SAVE_CAR_CREATED[MECHA_CAR_NUM] = 0
                    
                    wait 100
                                            
                    car.PutAt($USED_CAR[MECHA_CAR_NUM],0.0,3000.0,0.0)
                    car.Destroy($USED_CAR[MECHA_CAR_NUM])
                    
                    wait 3000
                    jump to @MECHANIC_MENU_EXIT
                    end
                end
            else
            00BC: show_text_highpriority GXT 'INV274' time 2000 flag 1
            05C1: AS_actor MECHA_ACTOR speak_from_audio_table 161
            end
        end
    end                                                         

if or
00E1:  player 0 pressed_key 15 
actor.Dead(PLAYER_ACTOR)
$HUNTER_ON_PLAYER == 1
0818:  actor PLAYER_ACTOR in_air
then
:MECHANIC_MENU_EXIT
03E6: remove_text_box 
08DA: remove_panel $PANEL    
00BE: text_clear_all    
$PANEL_OPEN = 0
return
else
jump @MECHANIC_MENU_IDLE
end        

jump to @MECHANIC_MENU_IDLE

:MECHA_SELECT_CAR_MENU_START2

if or
$FREEMODE == 1
$GAMEMODE_WAR == 1
then
camera.OnPed(MECHA_ACTOR,15,1)
end

if or
$FREEMODE == 1
$GAMEMODE_WAR == 1
then
else
MECHA_CAR_NUM = 0

if
$SAVE_CAR_CREATED[0] == 1
    then
    if
    056E:   car $USED_CAR[0] defined
        then
        if
        81AD: not  car $USED_CAR[0] sphere 0 near_point MECHA_STAND_X MECHA_STAND_Y radius 6.0 6.0
            then
            
            text.ClearAll()
            $PANEL_OPEN = 1
            $PHRASES_OFF_FLAG = 1
           
            text.ShowH('GAME707',2000,1)
            wait 2000
            
            text.ClearAll()
            $PANEL_OPEN = 0
            $PHRASES_OFF_FLAG = 0
            return
            
            else
            camera.OnVehicle($USED_CAR[MECHA_CAR_NUM],15,1)
            0@ = 1            
            end
        else
        
        text.ClearAll()
        $PANEL_OPEN = 1
        $PHRASES_OFF_FLAG = 1
        
        text.ShowH('GAME713',2000,1)
        wait 2000
        
        text.ClearAll()
        $PANEL_OPEN = 0
        $PHRASES_OFF_FLAG = 0
        return
        
        end                                
    else
    
    text.ClearAll()
    $PANEL_OPEN = 1
    $PHRASES_OFF_FLAG = 1
                            
    text.ShowH('GAME712',2000,1)
    wait 2000
    
    text.ClearAll()
    $PANEL_OPEN = 0
    $PHRASES_OFF_FLAG = 0
    return
                             
    end

return
end

text.ClearAll()

$PANEL_OPEN = 1
$PHRASES_OFF_FLAG = 1

06D2: 7@v = "GAME630" // @v = string
06D2: 9@v = "GAME630" // @v = string
06D2: 11@v = "GAME630" // @v = string
06D2: 13@v = "GAME630" // @v = string
06D2: 15@v = "GAME630" // @v = string

if
$SAVE_CAR_MODEL[0] > 0
then
0ADB: 7@v = car_model $SAVE_CAR_MODEL[0] name
end

if
$SAVE_CAR_MODEL[1] > 0
then
0ADB: 9@v = car_model $SAVE_CAR_MODEL[1] name
end

if
$SAVE_CAR_MODEL[2] > 0
then
0ADB: 11@v = car_model $SAVE_CAR_MODEL[2] name
end

if
$SAVE_CAR_MODEL[3] > 0
then
0ADB: 13@v = car_model $SAVE_CAR_MODEL[3] name
end

if
$SAVE_CAR_MODEL[4] > 0
then
0ADB: 15@v = car_model $SAVE_CAR_MODEL[4] name
end

0512: show_permanent_text_box 'GAME634'
$PANEL = 0
08D4: $PANEL = create_panel_with_title 'GAME554' position $X_MENU_SELECT_GAMEMODE $Y_MENU_SELECT_GAMEMODE width $WIDTH_MENU_SELECT_GAMEMODE columns 1 interactive 1 background 1 alignment 1  
08DB: set_panel $PANEL column 0 header 'GAME628' data 7@v 9@v 11@v 13@v 15@v 'DUMMY' 'DUMMY' 'DUMMY' 'DUMMY' 'DUMMY' 'DUMMY' 'DUMMY'
09DB: set_panel $PANEL column 0 width 130 
090E: set_panel $PANEL active_row $PANEL_CS_ACTIVE_ROW
wait 100

:MECHA_SELECT_CAR_MENU_IDLE
wait 0

$PHRASES_OFF_FLAG = 1

if
8A2A: not  text_box 'GAME634' displayed
then
0512: show_permanent_text_box 'GAME634' 
end      

08D7: $PANEL_CS_ACTIVE_ROW = panel $PANEL active_row 
    
if
00E1: player 0 pressed_key 16 
    then
    if
    $SAVE_CAR_CREATED[$PANEL_CS_ACTIVE_ROW] == 1
        then
        if
        056E:   car $USED_CAR[$PANEL_CS_ACTIVE_ROW] defined
            then
            if
            01AD:   car $USED_CAR[$PANEL_CS_ACTIVE_ROW] sphere 0 near_point MECHA_STAND_X MECHA_STAND_Y radius 6.0 6.0
                then
                008B: MECHA_CAR_NUM = $PANEL_CS_ACTIVE_ROW // (int)
                0@ = 1
                03E6: remove_text_box 
                08DA: remove_panel $PANEL
                camera.OnVehicle($USED_CAR[$PANEL_CS_ACTIVE_ROW],15,1)                                     
                return
                else
                text.ShowH('GAME707',2000,1)
                end
            else
            text.ShowH('GAME635',2000,1)
            end                                
        else                        
        text.ShowH('GAME632',2000,1)                         
        end
    end                                                                

if or
00E1:  player 0 pressed_key 15 
actor.Dead(PLAYER_ACTOR)
$HUNTER_ON_PLAYER == 1
0818:  actor PLAYER_ACTOR in_air
then
:MECHA_SELECT_CAR_MENU_EXIT
03E6: remove_text_box 
08DA: remove_panel $PANEL    
00BE: text_clear_all    
$PANEL_OPEN = 0
return
else
jump @MECHA_SELECT_CAR_MENU_IDLE
end        

jump to @MECHA_SELECT_CAR_MENU_IDLE

:RANDOM_DIALOG_MECHA_BYE
    
for 1@ = 0 to 200

if
MECHA_PEDTYPE == 26
then
v$PHRASE1 = "PH115_"
else
v$PHRASE1 = "BH115_"
end

0AD3: v$PHRASE2 = format "%d" 1@
098B: v$PHRASE3 = v$PHRASE1 + v$PHRASE2

09FD: get_gxt_string v$PHRASE3 width_to 31@

if
not 31@ == 0
    then    
    1@ += 1
    random_int 30@ 0 1@
    end

end  

if
MECHA_PEDTYPE == 26
then
v$PHRASE1 = "PH115_"
else
v$PHRASE1 = "BH115_"
end

0AD3: v$PHRASE2 = format "%d" 30@
098B: v$PHRASE3 = v$PHRASE1 + v$PHRASE2

return

:RANDOM_DIALOG_MECHA_BYE_BREYER
    
for 1@ = 0 to 200

v$PHRASE1 = "BR115_"

0AD3: v$PHRASE2 = format "%d" 1@
098B: v$PHRASE3 = v$PHRASE1 + v$PHRASE2

09FD: get_gxt_string v$PHRASE3 width_to 31@

if
not 31@ == 0
    then    
    1@ += 1
    random_int 30@ 0 1@
    end

end  

v$PHRASE1 = "BR115_"

0AD3: v$PHRASE2 = format "%d" 30@
098B: v$PHRASE3 = v$PHRASE1 + v$PHRASE2

return

:ENGINE_PRICE_SET

if
$MONTHS_PASSED > 0
then

for 1@ = 1 to $MONTHS_PASSED
MECHA_ENGINE_PRICE += 950
end

end

return

:MECHA_CAR_COLS_CHECK_SA

if and
UNITED_MAP == 1
LCVC_MAP_NUM2 == 1
then
jump to @MECHA_CAR_COLS_CHECK_III
end

if and
UNITED_MAP == 1
LCVC_MAP_NUM2 == 2
then
jump to @MECHA_CAR_COLS_CHECK_VC
end

if and
UNITED_MAP == 1
LCVC_MAP_NUM2 == 3
then
jump to @MECHA_CAR_COLS_CHECK_LCS
end

if and
UNITED_MAP == 1
LCVC_MAP_NUM2 == 4
then
jump to @MECHA_CAR_COLS_CHECK_VCS
end

12@ = 1

if or 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 401 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 402 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 404 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 405 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 409 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 410 
then 
12@ = 1
end 

if or 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 411 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 412 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 413 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 415 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 418 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 419 
then 
12@ = 1
end 

if or 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 421 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 426 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 434 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 436 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 440 
then 
12@ = 1
end 

if or 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 444 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 445 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 451 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 457 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 458 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 461 
then 
12@ = 1
end 

if or 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 462 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 463 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 468 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 474 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 475 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 477 
then 
12@ = 1
end 

if or 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 478 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 481 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 482 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 500 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 506 
then 
12@ = 1
end 

if or 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 507 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 510 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 516 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 518 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 521 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 526 
then 
12@ = 1
end 

if or 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 527 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 529 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 530 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 531 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 533 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 534 
then 
12@ = 1
end 

if or 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 539 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 540 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 543 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 545 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 546 
then 
12@ = 1
end 

if or 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 547 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 550 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 551 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 552 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 555 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 556 
then 
12@ = 1
end 

if or 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 559 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 560 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 561 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 509 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 562 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 558 
then 
12@ = 1
end 

if or 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 565 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 571 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 572 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 574 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 579 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 580 
then 
12@ = 1
end 

if or 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 581 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 582 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 585 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 586 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 589 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 543 
then 
12@ = 1
end 

if or 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 587 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 600 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 602 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 420 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 400 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 439 
then 
12@ = 1
end 

if or 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 535 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 542 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 549 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 438 
then 
12@ = 1
end 

if or 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 422 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 423 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 424 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 429 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 442 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 536 
then 
12@ = 2
end 

if or 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 466 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 467 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 471 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 479 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 483 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 489 
then 
12@ = 2
end 

if or 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 491 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 492 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 494 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 495 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 496 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 502 
then 
12@ = 2
end 

if or 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 503 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 504 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 517 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 522 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 525 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 480 
then 
12@ = 2
end 

if or 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 541 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 517 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 554 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 567
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 428 
then 
12@ = 2
end 

if or 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 573 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 575 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 576 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 583 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 603 
then 
12@ = 2
end

if or 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 416 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 596 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 599 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 597 
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 598  
then 
12@ = 2
end

if or
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 523
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 448
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 568
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 542
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 500
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 404
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 549
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 499
then 
12@ = 2
end

if or
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 498
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 524
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 416
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 431
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 427
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 490
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 407
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 544
then 
12@ = 2
end  

if or
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 557
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 15405
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 15408
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 15416
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 15419
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 15422
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 15423
then 
12@ = 2
end

if or
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 15430
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 15431
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 15432
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 15433
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 15435
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 15409
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 15406
then 
12@ = 2
end

if or
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 15473
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 15478
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 15479
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 15480
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 15482
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 15486
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 15484
then 
12@ = 2
end

if or
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 15485
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 539
$SAVE_CAR_MODEL[MECHA_CAR_NUM] == 15490
then 
12@ = 2
end  

return

:MECHA_CAR_COLS_CHECK_III

12@ = 2

if or
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15359
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15366
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15369
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15377
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15391
then
12@ = 1
end

return

:MECHA_CAR_COLS_CHECK_VC

12@ = 2

if or
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15265
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15267
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15268
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15273
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15275
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15278
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15279
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15283
then
12@ = 1
end

if or
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15284
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15285
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15289
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15290
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15293
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15294
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15296
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15307
then
12@ = 1
end

if or
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15309
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15310
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15312
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15313
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15314
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15319
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15320
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15323
then
12@ = 1
end

if or
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15328
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15329
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15332
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15333
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15334
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15335
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15337
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15342
then
12@ = 1
end

if or
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15343
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15344
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15345
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15263
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15250
then
12@ = 1
end

return

:MECHA_CAR_COLS_CHECK_LCS

12@ = 2

if or
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15417
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15418
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15420
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15421
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15422
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15435
then
12@ = 1
end

if or
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15437
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15440
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15469
then
12@ = 1
end

return

:MECHA_CAR_COLS_CHECK_VCS

12@ = 2

if or
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15487
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15492
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15493
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15495
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15500
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15501
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15502
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15507
then
12@ = 1
end

if or
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15508
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15509
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15510
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15513
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15514
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15516
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15518
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15520
then
12@ = 1
end

if or
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15522
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15523
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15527
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15529
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15531
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15532
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15534
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15535
then
12@ = 1
end

if or
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15536
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15537
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15538
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15539
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15540
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15542
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15543
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15544
then
12@ = 1
end

if or
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15545
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15548
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15550
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15551
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15552
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15554
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15557
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15559
then
12@ = 1
end

if or
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15563
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15565
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15566
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15571
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15578
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15579
then
12@ = 1
end

return

:MECHA_PAINT_NOT_AVALIABLE_CHECK

0@ = 1

if
UNITED_MAP == 0
then
gosub @MECHA_PAINT_NOT_AVALIABLE_CHECK_SA
end

if and
UNITED_MAP == 1
LCVC_MAP_NUM == 0
then
jump to @MECHA_PAINT_NOT_AVALIABLE_CHECK_III
end

if and
UNITED_MAP == 1
LCVC_MAP_NUM == 1
then
jump to @MECHA_PAINT_NOT_AVALIABLE_CHECK_VC
end

if and
UNITED_MAP == 1
STORIES_MAP == 1
LCVC_MAP_NUM == 0
then
jump to @MECHA_PAINT_NOT_AVALIABLE_CHECK_LCS
end

if and
UNITED_MAP == 1
STORIES_MAP == 1
LCVC_MAP_NUM == 1
then
jump to @MECHA_PAINT_NOT_AVALIABLE_CHECK_VCS
end

return

:MECHA_PAINT_NOT_AVALIABLE_CHECK_SA

if or
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15411
car.Model($USED_CAR[MECHA_CAR_NUM]) == #HOTKNIFE
then
0@ = 0
end

return

:MECHA_PAINT_NOT_AVALIABLE_CHECK_III

if or
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15350
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15372
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15370
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15365
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15405
then
0@ = 0
end

return

:MECHA_PAINT_NOT_AVALIABLE_CHECK_VC

if or
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15269
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15281
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15287
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15295
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15299
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15302
then
0@ = 0
end

if or
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15318
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15327
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15331
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15336
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15251
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15241
then
0@ = 0
end

return

:MECHA_PAINT_NOT_AVALIABLE_CHECK_LCS

if or
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15406
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15408
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15412
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15413
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15415
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15416
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15430
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15433
then
0@ = 0
end

if or
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15436
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15441
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15442
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15443
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15454
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15455
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15456
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15457
then
0@ = 0
end

if or
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15458
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15459
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15462
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15464
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15465
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15466
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15468
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15475
then
0@ = 0
end

return

:MECHA_PAINT_NOT_AVALIABLE_CHECK_VCS

if or
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15491
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15555
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15562
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15576
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15577
car.Model($USED_CAR[MECHA_CAR_NUM]) == 15598
then
0@ = 0
end

return        