:REMAPSET
thread 'REMAPSET'

if
1@ < -2
then
1@ = -1
end

if
0AA2: 31@ = load_library "PedFuncs.asi" // IF and SET
    then
    if
    0AA4: 30@ = get_proc_address "Ext_SetPedRemap" library 31@ // IF and SET
        then
        0A96: 3@ = actor 0@ struct               
        0AA5: call 30@ num_params 3 pop 3 1@ 2@ 3@
        0AA3: free_library 31@        
        end
    end 
    
end_thread