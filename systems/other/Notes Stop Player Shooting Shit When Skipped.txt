:NTWTCH
thread 'NTWTCH'

:NOTES_BLOCK_SHOOT_WATCHER_START

repeat
wait 0

if or
$READING_NOTE_FLAG == 1
BLACK_MENU_OPENED_FLAG == 1
    then
    break
    end

until false //$READING_NOTE_FLAG == 1

repeat
wait 0
ANTI_LEFT_STENKA_FLAG = 1

if or
$READING_NOTE_FLAG == 1
BLACK_MENU_OPENED_FLAG == 1
    then
    //ass
    else
    break
    end

until false //$READING_NOTE_FLAG == 0

32@ = 0

repeat
wait 0
//0AD1: show_formatted_text_highpriority "block cock" time 500
ANTI_LEFT_STENKA_FLAG = 1
until 32@ > 1000

ANTI_LEFT_STENKA_FLAG = 0

jump to @NOTES_BLOCK_SHOOT_WATCHER_START

end_thread